{% extends 'base.html.twig' %}

{% block title %}Nueva publicación{% endblock %}

{% block body %}
{{ include('navbar.html.twig') }}

<!-- Hero Section -->
<section class="relative bg-gradient-to-r from-purple-500 via-pink-500 to-indigo-600 py-16 text-white overflow-hidden">
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="absolute -right-10 -top-10 w-40 h-40 bg-white/10 rounded-full"></div>
    <div class="absolute -left-10 -bottom-10 w-40 h-40 bg-white/10 rounded-full"></div>
    
    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-3xl mx-auto text-center animate__animated animate__fadeInUp">
            <h1 class="text-4xl md:text-5xl font-bold mb-4">Comparte tu Historia del Carnaval</h1>
            <p class="text-xl opacity-90">Preserva y comparte los recuerdos más valiosos de esta celebración patrimonio de la humanidad</p>
        </div>
    </div>
</section>

<!-- Main Form Section -->
<section class="py-12 bg-gradient-to-br from-gray-50 to-indigo-50">
    <div class="container mx-auto px-4">
        <div class="max-w-2xl mx-auto">
            <!-- Progress Steps -->
            <div class="mb-10">
                <div class="flex justify-between items-center">
                    <div class="flex flex-col items-center">
                        <div class="w-10 h-10 rounded-full bg-indigo-500 text-white flex items-center justify-center font-bold mb-2 shadow-lg">1</div>
                        <span class="text-sm font-medium text-indigo-600">Información</span>
                    </div>
                    <div class="h-1 flex-1 bg-indigo-200 mx-2"></div>
                    <div class="flex flex-col items-center">
                        <div class="w-10 h-10 rounded-full bg-indigo-200 text-white flex items-center justify-center font-bold mb-2">2</div>
                        <span class="text-sm font-medium text-gray-500">Multimedia</span>
                    </div>
                    <div class="h-1 flex-1 bg-gray-200 mx-2"></div>
                    <div class="flex flex-col items-center">
                        <div class="w-10 h-10 rounded-full bg-gray-200 text-white flex items-center justify-center font-bold mb-2">3</div>
                        <span class="text-sm font-medium text-gray-500">Publicar</span>
                    </div>
                </div>
            </div>
            
            <!-- Form Card -->
            <div class="card bg-white shadow-2xl rounded-2xl overflow-hidden border-0 transform transition-all duration-300 hover:shadow-xl animate__animated animate__fadeIn">
                <div class="bg-gradient-to-r from-pink-500 to-purple-600 py-4 px-6">
                    <h2 class="text-2xl font-bold text-white flex items-center">
                        <span class="text-3xl mr-3">📝</span> 
                        Crear Nuevo Recuerdo
                    </h2>
                    <p class="text-pink-100 mt-1">Comparte tu experiencia en el Carnaval de Negros y Blancos</p>
                </div>
                
                <div class="card-body p-6 md:p-8">
                    {{ form_start(form, {'attr': {'enctype': 'multipart/form-data', 'class': 'space-y-6'}}) }}

                    <!-- Título Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.titulo, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                            <span class="ml-1 text-red-500">*</span>
                        </div>
                        <div class="relative">
                            {{ form_widget(form.titulo, {'attr': {
                                'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 pl-12',
                                'placeholder': 'Ej: Mi primer desfile de carrozas 2024'
                            }}) }}
                            <div class="absolute left-4 top-1/2 transform -translate-y-1/2 text-purple-500">
                                <i class="fas fa-heading text-lg"></i>
                            </div>
                        </div>
                        <p class="text-sm text-gray-500 mt-2 ml-1">Un título descriptivo que capture la esencia de tu recuerdo</p>
                    </div>

                    <!-- Descripción Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.descripcion, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                            <span class="ml-1 text-red-500">*</span>
                        </div>
                        <div class="relative">
                            {{ form_widget(form.descripcion, {'attr': {
                                'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 pl-12 min-h-[120px]',
                                'placeholder': 'Describe tu experiencia, emociones y detalles de este momento especial...',
                                'rows': '4'
                            }}) }}
                            <div class="absolute left-4 top-4 text-purple-500">
                                <i class="fas fa-align-left text-lg"></i>
                            </div>
                        </div>
                        <p class="text-sm text-gray-500 mt-2 ml-1">Comparte la historia detrás de este recuerdo carnavalesco</p>
                    </div>

                    

                    <!-- Multimedia Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.multimedia, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                        </div>
                        
                        <!-- File Upload Area -->
                        <div class="border-2 border-dashed border-gray-300 rounded-2xl p-6 text-center transition-all duration-300 hover:border-purple-400 hover:bg-purple-50" id="drop-area">
                            <div class="mb-4 text-purple-500">
                                <i class="fas fa-cloud-upload-alt text-4xl"></i>
                            </div>
                            <p class="text-lg font-medium text-gray-700 mb-2">Sube tus fotos o videos</p>
                            <p class="text-gray-500 mb-4">Arrastra y suelta archivos aquí o haz clic para seleccionar</p>
                            
                            <div class="relative inline-block">
                                {{ form_widget(form.multimedia, {'attr': {
                                    'class': 'absolute inset-0 w-full h-full opacity-0 cursor-pointer'
                                }}) }}
                                <button type="button" class="bg-gradient-to-r from-pink-500 to-purple-600 text-white px-6 py-3 rounded-xl font-medium shadow-md hover:shadow-lg transition-all duration-300">
                                    <i class="fas fa-folder-open mr-2"></i> Seleccionar archivos
                                </button>
                            </div>
                            
                            <p class="text-sm text-gray-500 mt-4">PNG, JPG, GIF, MP4 hasta 10MB</p>
                        </div>
                        
                        <!-- Preview Area -->
                        <div class="mt-4 grid grid-cols-3 gap-4 hidden" id="preview-area">
                            <!-- Preview items will be added here dynamically -->
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <div class="pt-4">
                        <button type="submit" class="w-full bg-gradient-to-r from-pink-500 to-purple-600 text-white py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-xl transform transition-all duration-300 hover:-translate-y-1 flex items-center justify-center group">
                            <span class="mr-2">Publicar Recuerdo</span>
                            <i class="fas fa-paper-plane group-hover:translate-x-1 transition-transform duration-300"></i>
                        </button>
                    </div>
{{ form_errors(form.multimedia) }}

                    {{ form_end(form) }}
                </div>
            </div>

            <!-- Back Link -->
            <div class="text-center mt-8 animate__animated animate__fadeInUp">
                <a href="{{ path('app_gallery_index') }}" class="inline-flex items-center text-purple-600 font-medium hover:text-purple-800 transition-colors duration-300 group">
                    <i class="fas fa-arrow-left mr-2 group-hover:-translate-x-1 transition-transform duration-300"></i>
                    Volver a la galería
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Tips Section -->
<section class="py-12 bg-white">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-10 text-gray-800">Consejos para una mejor publicación</h2>
        
        <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            <div class="bg-gradient-to-br from-pink-50 to-purple-50 p-6 rounded-2xl border border-pink-100 transform transition-all duration-300 hover:-translate-y-2 hover:shadow-lg">
                <div class="w-12 h-12 bg-gradient-to-r from-pink-500 to-purple-600 rounded-xl flex items-center justify-center text-white text-xl mb-4">
                    <i class="fas fa-camera"></i>
                </div>
                <h3 class="text-xl font-bold mb-3 text-gray-800">Imágenes de calidad</h3>
                <p class="text-gray-600">Utiliza fotos nítidas y bien iluminadas que capturen la esencia del carnaval. Las imágenes horizontales suelen verse mejor.</p>
            </div>
            
            <div class="bg-gradient-to-br from-blue-50 to-indigo-50 p-6 rounded-2xl border border-blue-100 transform transition-all duration-300 hover:-translate-y-2 hover:shadow-lg">
                <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-indigo-600 rounded-xl flex items-center justify-center text-white text-xl mb-4">
                    <i class="fas fa-pen-fancy"></i>
                </div>
                <h3 class="text-xl font-bold mb-3 text-gray-800">Descripciones detalladas</h3>
                <p class="text-gray-600">Comparte no solo lo que se ve, sino también las emociones, sonidos y experiencias que viviste durante el carnaval.</p>
            </div>
            
            <div class="bg-gradient-to-br from-green-50 to-teal-50 p-6 rounded-2xl border border-green-100 transform transition-all duration-300 hover:-translate-y-2 hover:shadow-lg">
                <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-teal-600 rounded-xl flex items-center justify-center text-white text-xl mb-4">
                    <i class="fas fa-tags"></i>
                </div>
                <h3 class="text-xl font-bold mb-3 text-gray-800">Categoriza correctamente</h3>
                <p class="text-gray-600">Selecciona la categoría que mejor represente tu contenido para que otros puedan encontrarlo más fácilmente.</p>
            </div>
        </div>
    </div>
</section>

{{ include('footer.html.twig') }}

<!-- JavaScript for Enhanced UI -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    // File upload preview functionality
    const dropArea = document.getElementById('drop-area');
    const fileInput = document.querySelector('input[type="file"]');
    const previewArea = document.getElementById('preview-area');
    
    // Prevent default drag behaviors
    ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
        dropArea.addEventListener(eventName, preventDefaults, false);
    });
    
    function preventDefaults(e) {
        e.preventDefault();
        e.stopPropagation();
    }
    
    // Highlight drop area when item is dragged over it
    ['dragenter', 'dragover'].forEach(eventName => {
        dropArea.addEventListener(eventName, highlight, false);
    });
    
    ['dragleave', 'drop'].forEach(eventName => {
        dropArea.addEventListener(eventName, unhighlight, false);
    });
    
    function highlight() {
        dropArea.classList.add('border-purple-500', 'bg-purple-100');
    }
    
    function unhighlight() {
        dropArea.classList.remove('border-purple-500', 'bg-purple-100');
    }
    
    // Handle dropped files
    dropArea.addEventListener('drop', handleDrop, false);
    
    function handleDrop(e) {
        const dt = e.dataTransfer;
        const files = dt.files;
        fileInput.files = files;
        handleFiles(files);
    }
    
    // Handle file input change
    fileInput.addEventListener('change', function() {
        handleFiles(this.files);
    });
    
    function handleFiles(files) {
        previewArea.innerHTML = '';
        previewArea.classList.remove('hidden');
        
        for (let i = 0; i < files.length; i++) {
            const file = files[i];
            
            if (!file.type.match('image.*') && !file.type.match('video.*')) {
                continue;
            }
            
            const reader = new FileReader();
            
            reader.onload = function(e) {
                const previewItem = document.createElement('div');
                previewItem.className = 'relative rounded-xl overflow-hidden bg-gray-100 aspect-square';
                
                if (file.type.match('image.*')) {
                    previewItem.innerHTML = `
                        <img src="${e.target.result}" class="w-full h-full object-cover" alt="Preview">
                        <div class="absolute top-2 right-2 bg-black/50 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs">
                            <i class="fas fa-image"></i>
                        </div>
                    `;
                } else if (file.type.match('video.*')) {
                    previewItem.innerHTML = `
                        <video class="w-full h-full object-cover">
                            <source src="${e.target.result}" type="${file.type}">
                        </video>
                        <div class="absolute top-2 right-2 bg-black/50 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs">
                            <i class="fas fa-video"></i>
                        </div>
                    `;
                }
                
                previewArea.appendChild(previewItem);
            };
            
            reader.readAsDataURL(file);
        }
    }
    
    // Form validation styling
    const form = document.querySelector('form');
    const inputs = form.querySelectorAll('input, textarea, select');
    
    inputs.forEach(input => {
        input.addEventListener('invalid', function() {
            this.classList.add('border-red-500', 'bg-red-50');
        });
        
        input.addEventListener('input', function() {
            if (this.checkValidity()) {
                this.classList.remove('border-red-500', 'bg-red-50');
                this.classList.add('border-green-500');
                
                // Remove green border after a while
                setTimeout(() => {
                    this.classList.remove('border-green-500');
                }, 2000);
            }
        });
    });
});
</script>

<style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');

body {
    font-family: 'Poppins', sans-serif;
}

.card {
    transition: all 0.3s ease;
}

.form-group input:focus, .form-group textarea:focus, .form-group select:focus {
    box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
}

/* Custom animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-fadeInUp {
    animation: fadeInUp 0.6s ease-out;
}
</style>
{% endblock %}