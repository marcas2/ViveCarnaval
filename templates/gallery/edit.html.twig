{% extends 'base.html.twig' %}

{% block title %}Editar Recuerdo{% endblock %}

{% block body %}
{{ include('navbar.html.twig') }}

<!-- Hero Section -->
<section class="relative bg-gradient-to-r from-amber-500 via-orange-500 to-pink-600 py-16 text-white overflow-hidden">
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="absolute -right-10 -top-10 w-40 h-40 bg-white/10 rounded-full"></div>
    <div class="absolute -left-10 -bottom-10 w-40 h-40 bg-white/10 rounded-full"></div>
    
    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-3xl mx-auto text-center animate__animated animate__fadeInUp">
            <h1 class="text-4xl md:text-5xl font-bold mb-4 flex items-center justify-center">
                <span class="text-4xl mr-4">✏️</span> 
                Editar Tu Recuerdo
            </h1>
            <p class="text-xl opacity-90">Actualiza y perfecciona este momento especial del Carnaval de Negros y Blancos</p>
        </div>
    </div>
</section>

<!-- Main Form Section -->
<section class="py-12 bg-gradient-to-br from-gray-50 to-amber-50">
    <div class="container mx-auto px-4">
        <div class="max-w-2xl mx-auto">
            <!-- Status Indicator -->
            <div class="mb-8 bg-white rounded-2xl shadow-lg p-6 border-l-4 border-amber-500">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-gradient-to-r from-amber-500 to-orange-500 rounded-full flex items-center justify-center text-white mr-4">
                        <i class="fas fa-info text-xl"></i>
                    </div>
                    <div>
                        <h3 class="text-lg font-bold text-gray-800">Editando publicación existente</h3>
                        <p class="text-gray-600">Estás modificando un recuerdo que ya forma parte de nuestra galería del Carnaval</p>
                    </div>
                </div>
            </div>
            
            <!-- Form Card -->
            <div class="card bg-white shadow-2xl rounded-2xl overflow-hidden border-0 transform transition-all duration-300 hover:shadow-xl animate__animated animate__fadeIn">
                <div class="bg-gradient-to-r from-amber-500 to-orange-600 py-4 px-6">
                    <h2 class="text-2xl font-bold text-white flex items-center">
                        <span class="text-3xl mr-3">📖</span> 
                        Actualizar Información del Recuerdo
                    </h2>
                    <p class="text-amber-100 mt-1">Mejora los detalles de tu experiencia carnavalesca</p>
                </div>
                
                <div class="card-body p-6 md:p-8">
                    {{ form_start(form, {'attr': {'enctype': 'multipart/form-data', 'class': 'space-y-6'}}) }}

                    <!-- Título Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.titulo, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                            <span class="ml-1 text-red-500">*</span>
                        </div>
                        <div class="relative">
                            {{ form_widget(form.titulo, {'attr': {
                                'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all duration-300 pl-12 bg-white',
                                'placeholder': 'Ej: Mi primer desfile de carrozas 2024'
                            }}) }}
                            <div class="absolute left-4 top-1/2 transform -translate-y-1/2 text-amber-500">
                                <i class="fas fa-heading text-lg"></i>
                            </div>
                        </div>
                        <p class="text-sm text-gray-500 mt-2 ml-1">Un título atractivo que capture la esencia de tu recuerdo</p>
                    </div>

                    <!-- Descripción Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.descripcion, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                            <span class="ml-1 text-red-500">*</span>
                        </div>
                        <div class="relative">
                            {{ form_widget(form.descripcion, {'attr': {
                                'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all duration-300 pl-12 min-h-[120px] bg-white',
                                'placeholder': 'Comparte la historia completa detrás de este momento especial...',
                                'rows': '4'
                            }}) }}
                            <div class="absolute left-4 top-4 text-amber-500">
                                <i class="fas fa-align-left text-lg"></i>
                            </div>
                        </div>
                        <p class="text-sm text-gray-500 mt-2 ml-1">Describe las emociones, sonidos y experiencias vividas</p>
                    </div>


                    

                    <!-- Multimedia Field -->
                    <div class="form-group">
                        <div class="flex items-center mb-2">
                            {{ form_label(form.multimedia, null, {'label_attr': {'class': 'block text-lg font-bold text-gray-800'}}) }}
                        </div>
                        
                        <!-- Current Files Preview -->
                        <div class="mb-4">
                            <h4 class="font-semibold text-gray-700 mb-3 flex items-center">
                                <i class="fas fa-images mr-2 text-amber-500"></i>
                                Archivos actuales
                            </h4>
                            <div class="bg-amber-50 border border-amber-200 rounded-xl p-4">
                                <p class="text-amber-700 text-sm mb-2">
                                    <i class="fas fa-info-circle mr-1"></i>
                                    Los archivos existentes se mantendrán a menos que subas nuevos
                                </p>
                                <div class="grid grid-cols-2 md:grid-cols-3 gap-3 mt-3">
                                    <!-- Placeholder for current media files -->
                                    <div class="bg-white rounded-lg p-3 border border-amber-300 text-center">
                                        <i class="fas fa-image text-amber-500 text-2xl mb-2"></i>
                                        <p class="text-xs text-gray-600">imagen_actual.jpg</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- File Upload Area -->
                        <div class="border-2 border-dashed border-gray-300 rounded-2xl p-6 text-center transition-all duration-300 hover:border-amber-400 hover:bg-amber-50" id="drop-area">
                            <div class="mb-4 text-amber-500">
                                <i class="fas fa-cloud-upload-alt text-4xl"></i>
                            </div>
                            <p class="text-lg font-medium text-gray-700 mb-2">Actualizar archivos multimedia</p>
                            <p class="text-gray-500 mb-4">Arrastra y suelta nuevos archivos o haz clic para seleccionar</p>
                            
                            <div class="relative inline-block">
                                {{ form_widget(form.multimedia, {'attr': {
                                    'class': 'absolute inset-0 w-full h-full opacity-0 cursor-pointer'
                                }}) }}
                                <button type="button" class="bg-gradient-to-r from-amber-500 to-orange-600 text-white px-6 py-3 rounded-xl font-medium shadow-md hover:shadow-lg transition-all duration-300">
                                    <i class="fas fa-sync-alt mr-2"></i> Actualizar archivos
                                </button>
                            </div>
                            
                            <p class="text-sm text-gray-500 mt-4">PNG, JPG, GIF, MP4 hasta 10MB</p>
                        </div>
                        
                        <!-- Preview Area -->
                        <div class="mt-4 grid grid-cols-2 md:grid-cols-3 gap-4 hidden" id="preview-area">
                            <!-- Preview items will be added here dynamically -->
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="pt-6 flex flex-col sm:flex-row gap-4">
                        <button type="submit" class="flex-1 bg-gradient-to-r from-amber-500 to-orange-600 text-white py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-xl transform transition-all duration-300 hover:-translate-y-1 flex items-center justify-center group">
                            <i class="fas fa-save mr-3 group-hover:scale-110 transition-transform duration-300"></i>
                            <span>Guardar Cambios</span>
                        </button>
                        
                        <a href="{{ path('app_gallery_index') }}" class="flex-1 bg-gradient-to-r from-gray-500 to-gray-700 text-white py-4 rounded-xl font-bold text-lg shadow-lg hover:shadow-xl transform transition-all duration-300 hover:-translate-y-1 flex items-center justify-center group text-center">
                            <i class="fas fa-times mr-3 group-hover:scale-110 transition-transform duration-300"></i>
                            <span>Cancelar</span>
                        </a>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="bg-white rounded-2xl shadow-lg p-6 border border-green-200">
                    <h3 class="text-lg font-bold text-gray-800 mb-3 flex items-center">
                        <i class="fas fa-rocket text-green-500 mr-2"></i>
                        Consejos para mejorar
                    </h3>
                    <ul class="text-sm text-gray-600 space-y-2">
                        <li class="flex items-start">
                            <i class="fas fa-check text-green-500 mr-2 mt-1"></i>
                            Revisa que el título sea claro y atractivo
                        </li>
                        <li class="flex items-start">
                            <i class="fas fa-check text-green-500 mr-2 mt-1"></i>
                            Añade detalles emocionales a la descripción
                        </li>
                        <li class="flex items-start">
                            <i class="fas fa-check text-green-500 mr-2 mt-1"></i>
                            Verifica que las imágenes sean de buena calidad
                        </li>
                    </ul>
                </div>
                
                <div class="bg-white rounded-2xl shadow-lg p-6 border border-blue-200">
                    <h3 class="text-lg font-bold text-gray-800 mb-3 flex items-center">
                        <i class="fas fa-shield-alt text-blue-500 mr-2"></i>
                        Estado de la publicación
                    </h3>
                    <div class="flex items-center text-sm text-gray-600">
                        <div class="w-3 h-3 bg-green-500 rounded-full mr-2 animate-pulse"></div>
                        <span>Esta publicación está actualmente <strong class="text-green-600">activa</strong> y visible para otros usuarios</span>
                    </div>
                    <p class="text-xs text-gray-500 mt-2">Última actualización: hace 2 días</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Statistics Section -->
<section class="py-12 bg-gradient-to-br from-orange-50 to-amber-100">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-10 text-gray-800">Tu Contribución al Carnaval</h2>
        
        <div class="grid md:grid-cols-4 gap-6 max-w-4xl mx-auto">
            <div class="bg-white rounded-2xl shadow-lg p-6 text-center transform transition-all duration-300 hover:-translate-y-2">
                <div class="w-16 h-16 bg-gradient-to-r from-amber-500 to-orange-500 rounded-full flex items-center justify-center text-white text-2xl mx-auto mb-4">
                    <i class="fas fa-eye"></i>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">156</h3>
                <p class="text-gray-600">Visualizaciones</p>
            </div>
            
            <div class="bg-white rounded-2xl shadow-lg p-6 text-center transform transition-all duration-300 hover:-translate-y-2">
                <div class="w-16 h-16 bg-gradient-to-r from-amber-500 to-orange-500 rounded-full flex items-center justify-center text-white text-2xl mx-auto mb-4">
                    <i class="fas fa-heart"></i>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">23</h3>
                <p class="text-gray-600">Me gusta</p>
            </div>
            
            <div class="bg-white rounded-2xl shadow-lg p-6 text-center transform transition-all duration-300 hover:-translate-y-2">
                <div class="w-16 h-16 bg-gradient-to-r from-amber-500 to-orange-500 rounded-full flex items-center justify-center text-white text-2xl mx-auto mb-4">
                    <i class="fas fa-comment"></i>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">7</h3>
                <p class="text-gray-600">Comentarios</p>
            </div>
            
            <div class="bg-white rounded-2xl shadow-lg p-6 text-center transform transition-all duration-300 hover:-translate-y-2">
                <div class="w-16 h-16 bg-gradient-to-r from-amber-500 to-orange-500 rounded-full flex items-center justify-center text-white text-2xl mx-auto mb-4">
                    <i class="fas fa-share"></i>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">12</h3>
                <p class="text-gray-600">Compartidos</p>
            </div>
        </div>
    </div>
</section>

{{ include('footer.html.twig') }}

<!-- JavaScript for Enhanced UI -->
<script>
document.addEventListener('DOMContentLoaded', function() {
    // File upload preview functionality
    const dropArea = document.getElementById('drop-area');
    const fileInput = document.querySelector('input[type="file"]');
    const previewArea = document.getElementById('preview-area');
    
    // Prevent default drag behaviors
    ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
        dropArea.addEventListener(eventName, preventDefaults, false);
    });
    
    function preventDefaults(e) {
        e.preventDefault();
        e.stopPropagation();
    }
    
    // Highlight drop area when item is dragged over it
    ['dragenter', 'dragover'].forEach(eventName => {
        dropArea.addEventListener(eventName, highlight, false);
    });
    
    ['dragleave', 'drop'].forEach(eventName => {
        dropArea.addEventListener(eventName, unhighlight, false);
    });
    
    function highlight() {
        dropArea.classList.add('border-amber-500', 'bg-amber-100');
    }
    
    function unhighlight() {
        dropArea.classList.remove('border-amber-500', 'bg-amber-100');
    }
    
    // Handle dropped files
    dropArea.addEventListener('drop', handleDrop, false);
    
    function handleDrop(e) {
        const dt = e.dataTransfer;
        const files = dt.files;
        fileInput.files = files;
        handleFiles(files);
    }
    
    // Handle file input change
    fileInput.addEventListener('change', function() {
        handleFiles(this.files);
    });
    
    function handleFiles(files) {
        previewArea.innerHTML = '';
        previewArea.classList.remove('hidden');
        
        for (let i = 0; i < files.length; i++) {
            const file = files[i];
            
            if (!file.type.match('image.*') && !file.type.match('video.*')) {
                continue;
            }
            
            const reader = new FileReader();
            
            reader.onload = function(e) {
                const previewItem = document.createElement('div');
                previewItem.className = 'relative rounded-xl overflow-hidden bg-gray-100 aspect-square';
                
                if (file.type.match('image.*')) {
                    previewItem.innerHTML = `
                        <img src="${e.target.result}" class="w-full h-full object-cover" alt="Preview">
                        <div class="absolute top-2 right-2 bg-black/50 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs">
                            <i class="fas fa-image"></i>
                        </div>
                        <div class="absolute bottom-2 left-2 bg-green-500 text-white text-xs px-2 py-1 rounded-full">
                            Nuevo
                        </div>
                    `;
                } else if (file.type.match('video.*')) {
                    previewItem.innerHTML = `
                        <video class="w-full h-full object-cover">
                            <source src="${e.target.result}" type="${file.type}">
                        </video>
                        <div class="absolute top-2 right-2 bg-black/50 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs">
                            <i class="fas fa-video"></i>
                        </div>
                        <div class="absolute bottom-2 left-2 bg-green-500 text-white text-xs px-2 py-1 rounded-full">
                            Nuevo
                        </div>
                    `;
                }
                
                previewArea.appendChild(previewItem);
            };
            
            reader.readAsDataURL(file);
        }
    }
    
    // Form validation styling
    const form = document.querySelector('form');
    const inputs = form.querySelectorAll('input, textarea, select');
    
    inputs.forEach(input => {
        // Add initial styling based on current value
        if (input.value) {
            input.classList.add('border-green-300', 'bg-green-50');
        }
        
        input.addEventListener('invalid', function() {
            this.classList.add('border-red-500', 'bg-red-50');
            this.classList.remove('border-green-300', 'bg-green-50');
        });
        
        input.addEventListener('input', function() {
            if (this.checkValidity()) {
                this.classList.remove('border-red-500', 'bg-red-50');
                this.classList.add('border-green-500', 'bg-green-50');
            }
        });
    });
    
    // Add character counter for description
    const descTextarea = document.querySelector('#{{ form.descripcion.vars.id }}');
    if (descTextarea) {
        const counter = document.createElement('div');
        counter.className = 'text-right text-sm text-gray-500 mt-1';
        counter.innerHTML = `<span id="char-count">${descTextarea.value.length}</span> caracteres`;
        
        descTextarea.parentNode.appendChild(counter);
        
        descTextarea.addEventListener('input', function() {
            document.getElementById('char-count').textContent = this.value.length;
        });
    }
});
</script>

<style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');

body {
    font-family: 'Poppins', sans-serif;
}

.card {
    transition: all 0.3s ease;
}

.form-group input:focus, .form-group textarea:focus, .form-group select:focus {
    box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
}

/* Custom animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-fadeInUp {
    animation: fadeInUp 0.6s ease-out;
}

/* Smooth transitions for all interactive elements */
button, a, input, textarea, select {
    transition: all 0.3s ease;
}
</style>
{% endblock %}